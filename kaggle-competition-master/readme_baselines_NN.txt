To run the baselines and neural network:

Run main.py under the competition folder.

Parameters are set in baseline.py
Handwritten code is in neural_network.py
Wrapper for sklearn is in neural_network_sk_comp.py
All the i/o is done in data.py

All the datasets required for main.py are in the Datasets folder